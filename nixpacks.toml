# Configuration for Railway Nixpacks build system
# Ensures ffmpeg, ffprobe, and system fonts are available in the container environment

[phases.setup]
nixPkgs = ["ffmpeg", "fontconfig", "dejavu_fonts"]
